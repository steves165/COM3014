/**
 * Javascript file for handling job entry.
 */

$(function() {
    $('#requiredSkills').inputList({'prefix': 'requiredSkills'});
    $('#desiredSkills').inputList({'prefix': 'desiredSkills'});
});